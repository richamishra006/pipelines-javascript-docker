
trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'dockerhub'
    repository: 'richamishra006 / maven-build'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
